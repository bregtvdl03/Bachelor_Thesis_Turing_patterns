Our goal in this section is to prove the following theorem.

\begin{theorem}{Existence and uniqueness of weak solutions}{eq_and_un}
    Let $u_0, u_{b, 0} \in \Ltwo{\partial \Omega}$ and $v_0 \in \Ltwo{\Omega}$ be non-negative initial conditions. Then there exist unique weak solutions $u, u_b \in \odespace$ and $v \in \pdespace$ to \cref{sys:ODEs,sys:PDEs,sys:Init}. Moreover, these solutions are also non-negative.
\end{theorem}

As previously mentioned, the proof will be based on an application of the Banach fixed-point theorem. If you have followed a course on ODE theory, chances are that you have seen the Picard-Lindel\"of Theorem and its proof. The proof for our model follows the same general idea, but requires some extra steps to handle our situation of a coupled system. We ``freeze'' the terms in the ODEs that depend on $v$ so we can use the standard existence and uniqueness properties we know about such systems to get solutions $u$ and $u_b$ to this alternative problem. The PDE gets a similar treatment, producing solutions $v$ of some alternative PDE, after which we can ``update'' the frozen terms in the ODE. This produces an operator $\mathcal{T}$ that maps any $v \in \pdespace$ to another $\widetilde{v} \in \pdespace$. If we can then show that this operator is a contraction, the Banach fixed-point theorem will guarantee the existence and uniqueness of a fixed point $v^* \in \pdespace$, which will be our solution to the PDE.

Let us first concretely demonstrate what we mean by ``freezing'' the terms that depend on $v$. Fix $v^* \in \pdespace$ and consider the modified system of ODEs given by

\begin{equation}\label{eq:frozen_odes}
    \begin{aligned}
        u_t         &= - f(u, u_b, \gamma v^*) + p(u)    & \text{on }& (0, T) \times \Gamma, \\
        u_{b,t}     &= f(u, u_b, \gamma v^*) + q(u_b)    & \text{on }& (0, T) \times \Gamma. \\
    \end{aligned}
\end{equation}
\todo{weak form?}

\noindent
These ODEs are now \emph{decoupled} from the PDE, so we can freely talk about the existence and uniqueness of solutions to this system without simultaneously having to worry about $v$ satisfying the PDE.

\begin{lemma}{Existence and uniqueness for the decoupled ODEs}{exun_frozen_odes}
    Let $u_0, u_{b,0} \in \Ltwo{\Gamma}$ be essentially bounded and non-negative initial conditions and fix $v^* \in \pdespace$. Then there exist unique weak solutions $u, u_b \in \odespace$ to \cref{eq:frozen_odes} satisfying these initial conditions. Moreover, these solutions are also non-negative and bounded by a constant depending on $T$.
\end{lemma}
\todo{correct addition to be ess bounded?}
\begin{proof}
    Fix $x$ and define $w$ and $F: \R \times \R^2 \to \R^2$ as in \cref{eq:weak_ode,eq:ode_system_function}, where $v$ is replaced by $\gamma v^*$. Since the components of $F$ are polynomial in $w$, it is certainly continuous in $w$. Additionally, by regularity of $\gamma v^*$\todo{time regularity of trace}, we know that $F$ is measurable in $t$. This also implies that for any compact subset of $\R \times \R^2$, we can bound $F$ with some time-dependent integrable function $m$. In particular, this subset can be taken to be a rectangle\footnote{On first sight, it can seem strange to call this type of set a rectangle instead of a cylinder. This name comes from the fact that in our main source \cite{coddington_theory_1984}, the theorem is proven for the case of $n = 1$. } as in the hypothesis of \cref{thm:caratheodory}, so let $R$ be such a rectangle around the initial time $t = 0$ and position $(u_0, u_{b,0})$. The hypothesis of \cref{thm:caratheodory} is then fully satisfied, giving us a local solution $w: [0, \beta] \to \R^2$ for some $\beta > 0$, with $w(0) = (u_0, u_{b,0})$.

    We show that this local solution is unique. Suppose we have two such local solutions, $w_1$ and $w_2$. Since these solutions are absolutely continuous, they are bounded on $[0, \beta]$\todo{right?}, so we can take a compact set $K \subseteq \R^2$ that contains both their trajectories. Again, $F$ is polynomial in $w$ and measurable in time, so it satisfies a Lipschitz-type condition

    \begin{equation*}
        \norm{F(t, \overline{w}) - F(t, \widetilde{w})} \leq L_K(t) \norm{\overline{w} - \widetilde{w}}
    \end{equation*}

    \noindent
    for all $t \in [0, \beta]$ and $\overline{w}, \widetilde{w} \in K$, where $L_K: [0, \beta] \to \R$ is an integrable function. Now the distance between the two solutions satisfies

    \begin{equation*}
        \begin{aligned}
            \norm{w_1(t) - w_2(t)} &= \norm{(u_0, u_{b,0}) + \int_{0}^{t} f(t, w_1(t)) \d t - (u_0, u_{b,0}) - \int_{0}^{t} f(t, w_2(t)) \d t} \\
            &\leq \int_{0}^{t} \norm{f(t, w_1(t)) - f(t, w_2(t))} \d t \\
            &\leq \int_{0}^{t} L_K(t) \norm{w_1(t) - w_2(t)} \d t .
        \end{aligned}
    \end{equation*}

    \noindent
    for every $t \in [0, \beta]$. Using Gronwall's inequality, we see that this implies that $\norm{w_1(t) - w_2(t)} = 0$ for all $t \in [0, \beta]$, so $w_1 = w_2$.

    To show that the components $u$ and $u_b$ of this solution $w$ are non-negative, we use \cref{thm:nagumo}. \todo{Lipschitz condition?}

    We now let $x$ vary and show that $u$ and $u_b$ have $L^2$ regularity in both space and time. \todo{regularity}

    We can use this non-negativity to show that $u$ and $u_b$ are bounded. Define $r := u + 2 u_b$ and note that

    \begin{equation*}
        r_t = u_t + 2 u_{b, t} = P_u - d_u u + (\alpha - 2 d_{u_b}) u_b \leq P_u + m r,
    \end{equation*}

    \noindent
    where $m := \max\left(d_u, \frac{1}{2}(\alpha - 2 d_{u_b})\right)$. Then Gronwall's inequality gives

    \begin{equation*}
        r(t) \leq r(0)e^{mt} + \frac{P_u}{m} \left(e^{mt} - 1\right).
    \end{equation*}

    \noindent
    Now the non-negativity of both $u$ and $u_b$ and the essential boundedness of $u_0$ and $u_{b,0}$ let us bound the solutions independently by

    \begin{equation*}
    \begin{alignedat}{2}
        u &\leq Me^{mT} + \frac{P_u}{m} \left(e^{mT} - 1\right) &&=: M_u(T) \qquad \text{and} \\
        u_b &\leq \frac{1}{2} M_u(T) &&=: M_{u_b}(T),
    \end{alignedat}
    \end{equation*}

    \noindent
    where $M = \esssup(u_0 + 2 u_{b, 0})$, for almost every $x \in \Gamma$ and $t \in [0, T]$.
\end{proof}

We can use a similar method for the PDE in \cref{sys:PDEs}. Fix $(u^*, u_b^*) \in \odespace^2$ and consider the modified weak formulation given by \todo{rewrite}

\begin{equation}\label{eq:frozen_pde}
    \begin{aligned}
        \inner{v_t, \phi}_{\Ltwo{\Omega}} &= - \inner{f(u^*, u_b^*, v), \phi}_{\Ltwo{\Gamma}} + \inner{r(v), \phi}_{\Ltwo{\Gamma}} - D \inner{\nabla v, \nabla \phi}_{\Ltwo{\Omega; \R^d}} \\
        &= \inner{g(v), \phi}_{\Ltwo{\Gamma}} - D \inner{\nabla v, \nabla \phi}_{\Ltwo{\Omega; \R^d}}, \\
    \end{aligned}
\end{equation}

\noindent
where $g(v) := - f(u^*, u_b^*, v) + r(v)$. Note that we can only make this definition because $u^*$ and $u_b^*$ are fixed. We have now decoupled the PDE from the ODEs, and we can prove a result that is nearly identical to \cref{lem:exun_frozen_odes}.

\begin{lemma}{Existence and uniqueness for the decoupled PDE}{exun_frozen_pde}
    Let $v_0 \in \Ltwo{\Omega}$ be non-negative initial conditions and fix $(u^*, u_b^*) \in \odespace^2$. Then there exists a unique weak solution $v \in \pdespace$ to \cref{eq:frozen_pde} satisfying these initial conditions. Moreover, this solution is also non-negative and bounded by a constant depending on $T$.
\end{lemma}

\begin{proof}
    \todo{Existence and Uniqueness}
    Now let $v$ be such a local solution. To show that $v$ is non-negative, we estimate the negative part of $v$. Let $v_{+}$ and $v_{-}$ be defined as $\max(0, v)$ and $- \min(0, v)$ respectively, such that $v = v_{+} - v_{-}$. Then we also have $v_t v_{-} = - (v_{-})_t v_{-}$, as well as $\nabla v \cdot \nabla v_{-} = - \norm{\nabla v_{-}}^2$, so \todo{formally say "for all $t$"} for all almost every $t$, we have,

    \begin{equation*}
         \frac{\d}{\d t} \frac12 \norm{v_{-}}_{\Ltwo{\Omega}}^2 =  \frac{\d}{\d t} \frac12 \int_{\Omega} v_{-}^2 = \int_\Omega (v_-)_t v_- = - \int_{\Omega} v_t v_{-}
    \end{equation*}

    \noindent
    due to the \todo{reference}. When we view $v_-$ as a test function and use that $v$ is a solution to the PDE, we get
    
    \begin{equation*}
        \begin{aligned}
         \frac{\d}{\d t} &\frac12 \norm{v_{-}}_{\Ltwo{\Omega}}^2 = - \int_{\Omega} v_t v_{-} = D_v \int_{\Omega} \nabla v \cdot \nabla v_- + d_v \int_{\Omega} v v_- - \int_{\Gamma} \left(P_v - \kon u^2 v + \koff u_b\right) v_-  \d S \\
        &= - D_v \norm{\nabla v_-}^2_{\Ltwo{\Omega}} - d_v \norm{v_-}^2_{\Ltwo{\Omega}} - P_v \int_{\Gamma} v_- \d S + \kon \int_{\Gamma} u^2 v v_-  \d S - \koff \int_{\Gamma} u_b v_-  \d S \\
        &= - D_v \norm{\nabla v_-}^2_{\Ltwo{\Omega}} - d_v \norm{v_-}^2_{\Ltwo{\Omega}} - P_v \int_{\Gamma} v_- \d S - \kon \int_{\Gamma} u^2 (v_-)^2  \d S - \koff \int_{\Gamma} u_b v_-  \d S \\
        &\leq 0,
    \end{aligned}
    \end{equation*}

    \noindent
    where we used the non-negativity of $v_-$ and $u_b$ in the last step. By \todo{reference}, we know that the map $t \mapsto \norm{v_{-}(t)}_{\Ltwo{\Omega}}^2$ should be continuous. The only way for this to be achieved while also guaranteeing the time derivative is non-positive, is by having

    \begin{equation*}
        \norm{v_{-}}_{\Ltwo{\Omega}}^2 = 0
    \end{equation*}

    \noindent
    for almost every $t$, and thus $v$ being non-negative.

    To show that the map $t \mapsto \norm{v}^2_{\Ltwo{\Omega}}$ is essentially bounded, we use $v$ itself as a test function to get

    \begin{equation*}
    \begin{aligned}
         \frac{\d}{\d t} \frac12 &\norm{v}^2_{\Ltwo{\Omega}} = \int_{\Omega} v_t v = \int_{\Gamma} \left(P_v - \kon u^2 v + \koff u_b\right) v \d S - D_v \int_{\Omega} \nabla v \cdot \nabla v - d_v \int_{\Omega} v v \\
        &= P_v \int_{\Gamma} v \d S - \kon \int_{\Gamma} u^2 v^2 \d S + \koff \int_{\Gamma} u_b v \d S  - D_v \norm{\nabla v}^2_{\Ltwo{\Omega}} - d_v \norm{v}^2_{\Ltwo{\Omega}} \\
        &\leq P_v \int_{\Gamma} v \d S + \koff \int_{\Gamma} u_b v \d S - D_v \norm{\nabla v}^2_{\Ltwo{\Omega}} . \\
    \end{aligned}
    \end{equation*}

    \noindent
    Now we apply Young's equality with $\varepsilon$ to the first two integrals in this last expression to derive   

    \begin{equation*}
    \begin{alignedat}{2}
        P_v \int_{\Gamma} v \d S &\leq \int_{\Gamma} \left(\varepsilon v^2 + \frac{1}{4 \varepsilon} P_v^2 \right) \d S = \varepsilon \norm{v}^2_{\Ltwo{\Gamma}} + \frac{1}{4\varepsilon} P_v^2 | \Gamma | &&=: \varepsilon \norm{v}^2_{\Ltwo{\Omega}} + \frac{1}{2}B_\varepsilon ,\\
        \koff \int_\Gamma u_b v &\leq \int_\Gamma \left(\varepsilon + \frac{1}{4 \varepsilon} \koff^2 u_b^2 \right) \d S = \varepsilon \norm{v}^2_{\Ltwo{\Gamma}} + \frac{1}{4\varepsilon} \koff^2 \norm{w}^2_{\Ltwo{\Gamma}} &&=: \varepsilon \norm{v}^2_{\Ltwo{\Gamma}} + \frac{1}{2} D_\varepsilon \norm{w}^2_{\Ltwo{\Gamma}} ,
    \end{alignedat}
    \end{equation*}

    \noindent
    which, when plugged into the original estimation, gives

    \begin{equation*}
         \frac{\d}{\d t} \frac12 \norm{v}^2_{\Ltwo{\Omega}} \leq \frac{1}{2} B_\varepsilon + \frac{1}{2} D_\varepsilon \norm{w}^2_{\Ltwo{\Gamma}} + 2 \varepsilon \norm{v}^2_{\Ltwo{\Gamma}} - D_v \norm{\nabla v}^2_{\Ltwo{\Omega}} .
    \end{equation*}

    \noindent
    To convert the $\Ltwo{\Gamma}$-norm of $v$ on the right-hand side into an $\Ltwo{\Omega}$-norm, we employ the trace inequality from \cref{thm:trace} and get

    \begin{equation*}
    \begin{aligned}
         \frac{\d}{\d t} \frac12 \norm{v}^2_{\Ltwo{\Omega}} &\leq \frac{1}{2} B_\varepsilon + \frac{1}{2} D_\varepsilon \norm{w}^2_{\Ltwo{\Gamma}} + 2 \varepsilon C_\gamma^2 \norm{v}^2_{H^1(\Omega)} - D_v \norm{\nabla v}^2_{\Ltwo{\Omega}} \\
        &= \frac{1}{2} B_\varepsilon + \frac{1}{2} D_\varepsilon \norm{w}^2_{\Ltwo{\Gamma}} + 2 \varepsilon C_\gamma^2 \norm{v}^2_{\Ltwo{\Omega}} + 2 \varepsilon C_\gamma^2 \norm{\nabla v}^2_{\Ltwo{\Omega}} - D_v \norm{\nabla v}^2_{\Ltwo{\Omega}} \\
        &\leq \frac{1}{2} B_\varepsilon + \frac{1}{2} D_\varepsilon \norm{w}^2_{\Ltwo{\Gamma}} + 2 \varepsilon C_\gamma^2 \norm{v}^2_{\Ltwo{\Omega}} \\
        &=: \frac{1}{2} B_\varepsilon + \frac{1}{2} D_\varepsilon \norm{w}^2_{\Ltwo{\Gamma}} + \frac{1}{2} A_\varepsilon \norm{v}^2_{\Ltwo{\Omega}}
    \end{aligned}
    \end{equation*}

    \noindent
    for small enough $\varepsilon > 0$ and almost every $t$. We are now ready to use Gronwall's inequality to get the final estimate

    \begin{equation*}
    \begin{aligned}
        \norm{v(t)}^2_{\Ltwo{\Omega}} &\leq \norm{v_0}^2_{\Ltwo{\Omega}} e^{A_\varepsilon t} + \int_{0}^{t} \left(B_\varepsilon + D_\varepsilon \norm{w(s)}^2_{\Ltwo{\Gamma}}\right) e^{A_\varepsilon (t - s)} \d s \\
        &= \norm{v_0}^2_{\Ltwo{\Omega}} e^{A_\varepsilon t} + \frac{B_\varepsilon}{A_\varepsilon} \left(e^{A_\varepsilon t} - 1\right) + D_\varepsilon \int_{0}^{t} \norm{w(s)}^2_{\Ltwo{\Gamma}} e^{A_\varepsilon (t - s)} \d s \\
        &\leq \norm{v_0}^2_{\Ltwo{\Omega}} e^{A_\varepsilon t} + \frac{B_\varepsilon}{A_\varepsilon} \left(e^{A_\varepsilon t} - 1\right) + \frac{D_\varepsilon M_{u_b}(T)^2 |\Gamma|}{A_\varepsilon} \left(e^{A_\varepsilon t} - 1\right) \\
        &\leq \norm{v_0}^2_{\Ltwo{\Omega}} e^{A_\varepsilon T} + \frac{B_\varepsilon}{A_\varepsilon} \left(e^{A_\varepsilon T} - 1\right) + \frac{D_\varepsilon M_{u_b}(T)^2 |\Gamma|}{A_\varepsilon} \left(e^{A_\varepsilon T} - 1\right) \\
        &=: M_v(T)
    \end{aligned}
    \end{equation*}

    \noindent
    for almost every $t$.
\end{proof}

Using these two lemmas, we can define two operators between $\pdespace$ and $\odespace^2$. One takes an arbitrary function $v^* \in \pdespace$ and maps it to the unique solution $(u, u_b) \in \odespace^2$ of \cref{eq:frozen_odes}. Let us refer to this operator as $\mathcal{T}_1: \pdespace \to \odespace^2$. The other, $\mathcal{T}_2: \odespace^2 \to \pdespace$, takes two functions $(u^*, u_b^*) \in \odespace^2$ and defines $\mathcal{T}_2(u^*, u_b^*)$ as the unique solution $v \in \pdespace$ to \cref{eq:frozen_pde}. Combining these two operators, we define

\begin{equation*}
    \mathcal{T} = \mathcal{T}_1 \circ \mathcal{T}_2 : \odespace^2 \to \odespace^2.
\end{equation*}

\noindent
We consider what it means for this operator to have a fixed point. That is, suppose we have functions $(u, u_b) \in \odespace^2$ such that $\mathcal{T}(u, u_b) = (u, u_b)$, and let $v \in \pdespace$ denote the intermediate value $v = \mathcal{T}_2(u, u_b)$. Given that then $\mathcal{T}_1(v) = (u, u_b)$, by definition this means that $v$ solves \cref{eq:weak_pde} for almost every $t \in (0, T)$, and $w = (u, u_b)$ solves \cref{eq:weak_ode} for almost every $t \in (0, T)$ and $x \in \Gamma$, meaning that $v$, $u$ and $u_b$ are weak solutions of our model. Conversely, if $v$, $u$ and $u_b$ are some weak solutions to the model, then by the uniqueness properties of \cref{lem:exun_frozen_odes,lem:exun_frozen_pde}, we must have that $\mathcal{T}_1(v) = (u, u_b)$ and $\mathcal{T}_2(u, u_b) = v$, and thus $\mathcal{T}(u, u_b) = (u, u_b)$.

It should be clear now why we are looking to apply the Banach fixed point theorem. If we prove that $\mathcal{T}$ is a contraction, we can guarantee the existence of a fixed point and therefore a weak solution. Since every weak solution of the model is also a fixed point, the theorem also grants us uniqueness of such solutions. To prove that $\mathcal{T}$ is a contraction, we first show this for $\mathcal{T}_1$ and $\mathcal{T}_2$ separately, after which we can easily combine the results.

\begingroup
\newcommand{\vb}{\overline{v}}
\newcommand{\vt}{\widetilde{v}}
\newcommand{\ub}{\overline{u}}
\newcommand{\ut}{\widetilde{u}}
\newcommand{\ubb}{\overline{u}_b}
\newcommand{\ubt}{\widetilde{u}_b}

\begin{lemma}{}{contraction}
    For $T > 0$ small enough, $\mathcal{T}_1: \pdespace \to \odespace^2$ is a contraction. That is, there is a $q \in [0, 1)$ such that
    
    \begin{equation*}
        \norm{\mathcal{T}_1 (\vb) - \mathcal{T}_1 (\vt)}_{\odespace^2}^2 \leq q \norm{\vb - \vt}_\pdespace^2
    \end{equation*}

    for every $\vb, \vt \in \pdespace$.
\end{lemma}

\begin{proof}
    Let $\vb, \vt \in \pdespace$ and define $\ub, \ubb, \ut$ and $\ubt$ by $(\ub, \ubb) := \mathcal{T}_1(\vb)$ and $(\ut, \ubt) := \mathcal{T}_1(\vt)$. We now denote their differences by

    \begin{equation*}
        v := \vb - \vt, \qquad u := \ub - \ut \qquad \text{and} \qquad u_b := \ubb - \ubt,
    \end{equation*}

    \noindent
    such that

    \begin{equation*}
        \norm{\mathcal{T}_1 (\vb) - \mathcal{T}_1 (\vt)}_{\odespace^2}^2 = \norm{(\ub, \ubt) - (\ut, \ubt)}_{\odespace^2}^2 = \norm{(u, u_b)}_{\odespace^2}^2 = \norm{u}_{\odespace}^2 + \norm{u_b}_{\odespace}^2.
    \end{equation*}

    \noindent
    We therefore seek to estimate these separate differences $u$ and $u_b$ in terms of $v$. Starting off with $u$, we see that it satisfies
    \todo{first step?}
    \begin{equation*}
    \begin{aligned}
        \frac{\d}{\d t} \norm{u}_{\Ltwo{\Gamma}}^2 &= 2 \int_\Gamma u_t u \d S \\
        &= 2 \int_\Gamma (\ub_t - \ut_t) u \d S \\
        &= 2 \int_\Gamma \left( \alpha (\ubb - \ubt) - d_u (\ub - \ut) - 2 \kon (\ub^2 \vb - \ut^2 \vt) + 2 \koff (\ubb - \ubt) \right) u \d S \\
        &= 2 (\alpha + 2 \koff) \int_\Gamma u u_b \d S - 2 d_u \int_\Gamma u^2 \d S - 4 \kon \int_\Gamma (\ub^2 \vb - \ut^2 \vt) u \d S
    \end{aligned}
    \end{equation*}

    \noindent
    for almost every $t$, at which point we use the identity

    \begin{equation} \label{eq:contraction_identity_1}
        \ub^2 \vb - \ut^2 \vt = \ub^2 \vb - \ut^2 \vb + \ut^2 \vb - \ut^2 \vt = (\ub^2 - \ut^2) \vb + \ut^2 v = (\ub + \ut) \vb u + \ut^2 v
    \end{equation}

    \noindent
    to further derive
    \todo{overflow}

    \begin{equation*}
    \begin{aligned}
        \frac{\d}{\d t} \norm{u}_{\Ltwo{\Gamma}}^2 &= 2 (\alpha + 2 \koff) \int_\Gamma u u_b \d S - 2 d_u \int_\Gamma u^2 \d S - 4 \kon \int_\Gamma (\ub + \ut) \vb u^2 \d S - 4 \kon \int_\Gamma \ut^2 v u \d S \\
        &\leq 2 (\alpha + 2 \koff) \int_\Gamma u u_b \d S - 2 d_u \int_\Gamma u^2 \d S + 4 \kon \int_\Gamma \left| (\ub + \ut) \vb \right| u^2 \d S + 4 \kon \int_\Gamma \ut^2 \left| v u \right| \d S \\
        &\leq 2(\alpha + 2 \koff) \int_\Gamma u u_b \d S + \left(8 \kon M_u (T) M_v (T) - 2 d_u\right) \norm{u}_{\Ltwo{\Gamma}}^2 + 4 \kon M_u (T)^2 \int_\Gamma | v u | \d S . \\
    \end{aligned}
    \end{equation*}
    
    \noindent
    To further separate the mixed terms, we use Young's inequality twice and estimate

    \begin{equation*}
    \begin{aligned}
        \frac{\d}{\d t} \norm{u}_{\Ltwo{\Gamma}}^2 &\leq A_1(T) \norm{u}_{\Ltwo{\Gamma}}^2 + A_2 \norm{u_b}_{\Ltwo{\Gamma}}^2 + B_1(T) \norm{v}_{\Ltwo{\Gamma}}^2 , \\
    \end{aligned}
    \end{equation*}

    \noindent
    where $A_1(T)$, $A_2$ and $B_1(T)$ are defined as

    \begin{equation*}
        A_1(T) := 8 \kon M_u (T) M_v (T) - 2 d_u + \alpha + 2 \koff + 2 \kon M_u (T)^2
    \end{equation*}
    \begin{equation*}
        A_2 := \alpha + 2 \koff \qquad \text{and} \qquad B_1(T) := 2 \kon M_u (T)^2.
    \end{equation*}

    \noindent
    Using the same arguments, we get a similar estimate for $\frac{\d}{\d t} \norm{u_b}_{\Ltwo{\Gamma}}^2$, namely

    \begin{equation*}
    \begin{aligned}
        \frac{\d}{\d t} \norm{u_b}_{\Ltwo{\Gamma}}^2 &= 2 \int_\Gamma \left(\left(\ubb\right)_t - \left(\ubt\right)_t\right) u_b \d S \\
        &= - 2 \left(d_{u_b} + \koff\right) \int_\Gamma u_b^2 \d S + 2 \kon \int_\Gamma \left(\ub^2 \vb - \ut^2 \vt\right) u_b \d S \\
        &= - 2 \left(d_{u_b} + \koff\right) \int_\Gamma u_b^2 \d S + 2 \kon \int_\Gamma \left(\ub + \ut\right) \vt u u_b \d S + 2 \kon \int_\Gamma \ut^2 v u_b \d S \\
        &\leq - 2 \left(d_{u_b} + \koff\right) \int_\Gamma u_b^2 \d S + 2 \kon \int_\Gamma \left(\ub + \ut\right) \vt \left| u u_b \right| \d S + 2 \kon \int_\Gamma \ut^2 \left| v u_b \right| \d S \\
        &\leq - 2 \left(d_{u_b} + \koff\right) \int_\Gamma u_b^2 \d S + 4 \kon M_u(T) M_v(T) \int_\Gamma \left| u u_b \right| \d S + 2 \kon M_u(T)^2 \int_\Gamma \left| v u_b \right| \d S \\
        &\leq A_3(T) \norm{u_b}_{\Ltwo{\Gamma}}^2 + A_4(T) \norm{u}_{\Ltwo{\Gamma}}^2 + B_2(T) \norm{v}_{\Ltwo{\Gamma}}^2 ,\\
    \end{aligned}
    \end{equation*}

    \noindent
    where $A_3(T)$, $A_4(T)$ and $B_2(T)$ are given by

    \begin{equation*}
        A_3(T) := 2 \kon M_u(T) M_v(T) + \kon M_u(T)^2 - 2 d_{u_b} - 2 \koff ,
    \end{equation*}
    \begin{equation*}
        A_4(T) := 2 \kon M_u(T) M_v(T) \qquad \text{and} \qquad B_2(T) := \kon M_u(T)^2 .
    \end{equation*}

    \noindent
    To merge these estimates, we let $A(T) := 2 \max \left(A_1(T), A_2, A_3(T), A_4(T)\right)$ and $B(T) := 2 \max \left(B_1(T), B_2(T)\right)$ and sum to get \todo{overflow}

    \begin{equation*}
        \frac{\d}{\d t} \left(\norm{u}_{\Ltwo{\Gamma}}^2 + \norm{u_b}_{\Ltwo{\Gamma}}^2\right) \leq A(T) \left(\norm{u}_{\Ltwo{\Gamma}}^2 + \norm{u_b}_{\Ltwo{\Gamma}}^2\right) + B(T) \norm{v}_{L^2(\Gamma)}^2
    \end{equation*}

    \noindent
    for almost all $t$. An application of Gronwall's inequality gives \todo{Trace inequality?}

    \begin{equation*}
    \begin{aligned}
        \norm{u(t)}_{\Ltwo{\Gamma}}^2 + \norm{u_b(t)}_{\Ltwo{\Gamma}}^2 &\leq \norm{u(0)}_{\Ltwo{\Gamma}}^2 + \norm{u_b(0)}_{\Ltwo{\Gamma}}^2 + B(T) \int_{0}^{t} \norm{v(s)}_{\Ltwo{\Gamma}}^2 e^{(t - s) A(T)} \d s \\
        &\leq B(T) e^{T A(T)} \norm{v}_{\Ltwo{0, T; \Ltwo{\Gamma}}}^2 , \\
    \end{aligned}
    \end{equation*}

    \noindent
    since the initial conditions of $\ub$ and $\ut$ are the same, and therefore $u(0) = 0$ for every $x \in \Gamma$. Naturally, the same holds for $u_b(0)$. After integrating from 0 to $T$, we can conclude that

    \begin{equation*}
        \norm{u}_{\Ltwo{0, T; \Ltwo{\Gamma}}}^2 + \norm{u_b}_{\Ltwo{0, T; \Ltwo{\Gamma}}}^2 \leq T B(T) e^{T A(T)} \norm{v}_{\Ltwo{0, T; \Ltwo{\Gamma}}}^2 .
    \end{equation*}

    \noindent
    
\end{proof}

\endgroup

We have now gathered enough results to prove the main statement of this section.

\begin{proof}[Proof of \cref{thm:eq_and_un}]
    Using \cref{lem:contraction}, let $T > 0$ such that $\mathcal{T}$ is a contraction. Then by the Banach fixed-point theorem, there exists a unique fixed point $v^*$ of $\mathcal{T}$. Next, define $(u^*, u_b^*) := \mathcal{T}_1 v^*$. Then by definition, $u^*$ and $u_b^*$ solve \cref{eq:frozen_odes} with $v^*$ as the frozen input. On the other hand, $\mathcal{T}_2 (u^*, u_b^*) = \mathcal{T}v^* = v^*$ solves \cref{eq:frozen_pde} with $u^*$ and $u_b^*$ as the inputs to the frozen reactions. In other words, $u^*$, $u_b^*$ and $v^*$ are weak solutions to \cref{sys:ODEs,sys:PDEs,sys:Init}.

    To show uniqueness of the weak solutions, it is enough to note that for any weak solution $u^*, u_b^*$ and $v^*$, we have that $v^*$ is a fixed point of $\mathcal{T}$. This follows directly from the uniqueness results from \cref{lem:exun_frozen_odes,lem:exun_frozen_pde}. Since the Banach fixed-point theorem tells us that fixed points are unique, we know that weak solutions to \cref{sys:ODEs,sys:PDEs,sys:Init} are too.
\end{proof}


% \begin{enumerate}
%     \item For every $v^* \in \mathcal{X}$, define $\mathcal{T}_1 (v^*)$ as the solution $(u^*, u_b^*) \in \mathcal{Y}^2$ to the ODEs~\ref{temp:ode} with frozen reactions.
%     \item For every $(u^*, u_b^*) \in \mathcal{Y}^2$, define $\mathcal{T}_2 (u^*, u_b^*)$ as the solution $v^* \in \mathcal{X}$ of the PDE~\ref{temp:pde} with frozen flux.
% \end{enumerate}

% We want to use the Picard-Lindel\"of theorem to prove existence and uniqueness of the ODEs. If we fix a solution of the PDEs $v$, we have a system

% \begin{equation}
%     \label{temp:ode}
%     \begin{aligned}
%         \partial_t u(t,x)    &= f(t, x, u(t,x), u_b(t,x)), \\
%         \partial_t u_b(t,x)  &= g(t, x, u(t,x), u_b(t,x)), \\
%     \end{aligned}
% \end{equation}

% \noindent
% where $f$ and $g$ both depend on the trace $v$. We need $f$ and $g$ to be Lipschitz continuous in $u$ and $u_b$ and continuous in time. The former follows from the definition of the model(???), but for the latter, we need continuity of $v$ on $\partial C$. In other words, for every $x \in \partial C$, we need the function $t \mapsto v \vert_{\partial C}(t, x)$ to be continuous. Then for every $x$, we can solve the system~\ref{temp:ode} for $u(\ \cdot\ , x)$ and $u_b(\ \cdot\ , x)$. What can we say about $u(t,\ \cdot\ )$ and $u_b(t,\ \cdot\ )$?

% For fixed $u$ and $u_b$, we have the following equations for $v$:

% \begin{equation}
%     \label{temp:pde}
%     \begin{aligned}
%         v_t(t, x) &= D\Delta v(t, x) && \text{on } (0, T) \times E,\\
%         D \pderiv[v]{\nu}(t, x) &= h(t, x, v(t, x)) && \text{on } (0, T) \times \partial C,\\
%         D \pderiv[v]{\nu}(t, x) &= 0 && \text{on } (0, T) \times \partial C,\\
%     \end{aligned}
% \end{equation}

% \noindent
% where $h$ depends on $u$ and $u_b$.

% \begin{equation*}
%     \begin{aligned}
%         \mathcal{X} &:= \left\{ v \in \Ltwo{0, T; \Omega} \cap C([0, T]; \Ltwo{\Omega}) \mid v \geq 0 \text{ and } v \vert_{\partial C}(\ \cdot\ , x) \in C([0, T]) \text{ for every } x \in \partial C \right\} \\
%         \mathcal{Y} &:= \left\{ u \in \Ltwo{0, T; H^1(\Omega)} \mid u \geq 0 \text{ and } u' \in \Ltwo{0, T; H^{-1}(\Omega)} \right\}
%     \end{aligned}
% \end{equation*}

% \noindent
% Questions:

% \begin{enumerate}
%     \item ODE is not Lipschitz, only locally?
%     \item Picard Lindel\"of for weak solutions?
%     \item Inward flow theorem for weak solutions?
%     \item When do we have time continuity of the trace?
%     \item System of ODEs $\rightarrow$ What can we say about $u(t,\ \cdot\ )$?
% \end{enumerate}